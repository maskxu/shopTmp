import { Dependencies } from '../types';
import { DocumentManager } from '../documents';
import { DiagnosticsManager } from './DiagnosticsManager';
export declare function makeRunChecks(documentManager: DocumentManager, diagnosticsManager: DiagnosticsManager, { loadConfig, findRootURI, fileSize, fileExists, getDefaultTranslationsFactory, getDefaultLocaleFactory, themeDocset, schemaValidators, }: Pick<Dependencies, 'loadConfig' | 'findRootURI' | 'fileExists' | 'fileSize' | 'getDefaultTranslationsFactory' | 'getDefaultLocaleFactory' | 'themeDocset' | 'schemaValidators'>): (triggerURIs: string[]) => Promise<void>;
